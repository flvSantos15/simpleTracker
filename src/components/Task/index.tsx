import React from 'react'
import {
  StyleSheet,
  View,
  Text,
  TouchableOpacity,
} from 'react-native'

export function Task(){
  return(
    <View>
      {/* nome da task */}
      {/* contador da task */}
      {/* botao de inicio */}
      {/* botao para finalizar */}
      {/* com uma funçao que ao ser finalizado vai voltar a tela inicial*/}
    </View>
  )
}

const stytes = StyleSheet.create({
  
})